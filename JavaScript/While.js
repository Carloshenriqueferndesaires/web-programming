const quantidade = function (indice) {
    while (indice < 10) {
        console.log(`Contagem: ${indice}`);
        indice++;
    }
}
quantidade(0);

function contagem(indice) {
do {
     console.log(`frequencia: ${indice}`);
        indice++;
}while (indice < 10);

}
contagem(0);